# FC-067 initial record store boundary

## LOG #175 result - the opaque transform is now linked

ACCEPTED in bounded self-review, restored verification passed; exact checkpoint
verification follows commit:
block8c03c93a executes eleven SHIL operations with nineteen live words and
eighteen dynamic events. FTRV at8c03c944 multiplies an actually loaded vector
by the sixteen observed XF words. Its first three outputs pass unchanged over
the immediate executed edge to8c03c94c and then through the existing initial
RAM/reciprocal/final-RAM/SQ/TA chain to opaque vertex4 of frame1782, T1401N.
This is the first linked transform for this selected opaque vertex, not a
camera inferred from a separate translucent effect or a shared buffer address.

The four sequential source loads are8c8ea410/14/18/1c and return
3e253606,be200615,bb2bd6ad,3f8005df. The four FTRV outputs are
c42bc1dc,c2252f6c,409052c6,3f8005df. Preserve the actual nonunit fourth
component; neither normalize it to1 nor infer a packed-field meaning.
The complete matrix words remain in private evidence and machine-readable
verifier output. Matrix origin/decomposition and source coordinate space
remain unknown. Source material/readback and world-camera packets are not
silently upgraded by this single-vertex diagnostic.

The verifier independently reproduces all four mathematical dot outputs from
exact binary32 input fractions under observed toward-zero rounding. This is
not a full instruction-level binary64 emulator or a universal FTRV proof.
Observed dynamic return values, not a mathematical estimate alone, bind the
transform to the actual successor inputs and initial RAM stores.

Two captures only: initial-supply-a positive and initial-supply-b-negative,
same diagnostic build, both three-frame native D3D11 and exit0/clean close.
The negative changes expected Z409052c6 to409052c7 at the next-entry checker;
actual registers/RAM remain unchanged and live/offline checks reject. Both
still pass the prior initial-store/reciprocal/RAM/SQ/TA verifier chain.
All54 unique image-plane,6 producer-identity and18 raw-guidance comparisons
match the retained hook-free56cfc50ba baseline.

Six offline controls falsify target edge, generation, expected output,
matrix translation, event sequence and vector SSA source. Seven synthetic
test methods include asymmetric independent goldens, nonunit W preservation,
mode/nonfinite faults, missing/duplicate observations, prior-log shadowing,
and a self-consistent alternative transform that fails successor binding.
Python128 inspector methods plus5 binary32 methods pass. Temporary core hooks
are removed; ignored initial-supply-a patch/header retain the exact experiment.
See LOG #175 for restored and exact-SHA build/checkpoint completion.

Restored validation: all four serial configurations build; the three enabled
selftests pass298/298 each; public SDK mock passes56/56 with runtime/GPU/Present
false. The core diff is empty. No third-party binary/configuration or game data
is included in this checkpoint. Exact-commit build/native verification follows.

Do not follow more predecessors automatically: this approved block succeeded
in finding the transform and has been exhausted. The next useful proposed
milestone is a finite opaque camera-contract investigation using this concrete
transform and additional bounded opaque samples, with explicit model/view/
projection separation or an unsupported result. That broader coverage is not
authorized by this one-block approval. Do not repeat the completed trace,
reopen parked one-LSB replay, or label full M2/Remix GPU/relighting complete.

## User-approved continuation - 2026-09-08

The user explicitly approved one preceding block supplying FR0-FR2 to
8c03c94c and requested continued work in Astra light without spinning wheels.
This supersedes the scope stop below only for that one block/edge. Starting
HEAD is56cfc50ba00302512103f831ed1a1474b7ea0193; preserve any newer work.
The completed M2 requirements audit is not the next task again.

Concrete deliverable: observe the actual immediately preceding executed block
and its handoff into the selected initial-store invocation. Explain which of
FR0-FR2 it calculates, loads or merely forwards. Prove available arithmetic or
load provenance with exact dynamic operands/results and falsifying controls;
retain unknown coordinate-space/camera labels unless this evidence establishes
them. A preceding branch or copy is an honest bounded result, not authority to
follow another predecessor. This does not shrink the full M2 acceptance goal.

Implementation bounds:

- Reuse the retained initial-record-b diagnostic patch/header and verifier
  chain. Do not recreate completed SQ, RAM or store discovery. Buffer only one
  predecessor witness for the active record lease; do not log a caller census.
- Select by the actual successor invocation, record destination/generation and
  execution edge, not PC/cycle coincidence alone. Preserve the current lease,
  reset/fallback rejection and no-rearm behavior. Keep the existing bounds of
  128 operations, 32 live words and 512 events; reject overflow instead of
  silently truncating or expanding them. No recursive upstream tracing.
- Test missing/intervening edge, stale generation, wrong operand/result and
  missing/duplicate events. A live expected-word-only negative must reject
  while leaving the game's registers/RAM untouched. Preserve failed attempts.
- Run one positive and same-build negative bounded three-frame native capture;
  allow a corrective rerun only for a diagnosed fixture defect. Compare the
  nine image planes, producer identity and three raw guidance planes per frame
  with the retained hook-free56cfc50ba baseline. No quality/performance claim.
- Remove temporary core hooks after the experiment. Run the existing serial
  build/selftest matrix and focused Python checks, checkpoint only owned
  verifier/tests/docs, then exact-SHA validation/push per repository discipline.
  No proprietary binary inspection or live external configuration edits.

Return an accepted bounded witness or a precise unsupported/failing result,
plus its impact on M2. Do not restart the same captures/audit when the block
does not resolve coordinate origin. Further upstream expansion stays outside
this approval. No additional agents; requested implementation is Astra low.

## Historical LOG #174 disposition

ACCEPTED in bounded self-review: actual live FR0/FR1/FR2 operands are copied
into the earlier XYZ record by block8c03c94c. NOT_REVIEWABLE within this block:
the calculation and coordinate system of those precomputed operands. No
transform, floating-point arithmetic or matrix operation is present. This is
a stopping boundary, not another automatic predecessor assignment. Starting
SHAd38c25f76. Overall M2 remains incomplete.

Temporary hooks/header removed; ignored initial-record-a/b patch/header pairs
retain the experiment. Only verifier/tests/docs are checkpointed. B and
C-negative share one build. See LOG #174 for restored and exact validation.

## Actual bounded witness

One invocation at cycle7602512960 has ten SHIL operations, five live-in scalar
words and ten dynamic events. Actual store destination8ce74258 qualifies the
buffered invocation; neighboring iterations are not accepted by timestamp.
The128-op/32-live-word/512-event bound is not exceeded or expanded.

| Live operand | Actual bits | Actual store |
|---|---|---|
|FR2|409052c6|PC8c03c94e ->8ce74258 (earlier Z)|
|FR1|c2252f6c|PC8c03c950 ->8ce74254 (earlier Y)|
|FR0|c42bc1dc|PC8c03c952 ->8ce74250 (earlier X)|

Entry r0=8ce7425c is predecremented for each store. Entry r6=78 is decremented
and compared to zero; the branch-condition result and final pointer increment
also replay exactly. There are no loads and no modifications to the three
floating-point coordinate operands. FPSCR/MXCSR are metadata only here; no
floating-point calculation is inferred from their presence.

The source operands match the actual initial RAM generation, which precedes the
already proven reciprocal-Z and final-coordinate blocks. The full prior chain
still passes through the selected opaque vertex. Initial-coordinate-calculation
and world-camera flags explicitly remain false. Sharing a RAM array with an
earlier effect does not supply missing coordinate-space provenance.

## Controls and preservation

A and B both pass the store verifier. B changes the diagnostic query target
from unobserved r4 to actual FR2; no negative run on the unused A selector is
claimed. C-negative changes only expected FR2 from409052c6 to409052c7. Actual
register, RAM, reciprocal calculation and scene remain unchanged; live and
offline rejection occur. Four offline controls mutate a self-consistent input,
store SSA source, destination and event sequence.

Five synthetic methods verify independent register/store goldens, controls
despite shadowing earlier RAM log lines, stale SSA/PC/pointer faults, duplicate
or missing events, live rejection and refusal to promote the block to float/
transform arithmetic. All A/B/C exit0/clean close. Each matches27 unique image
planes, three producer identities and nine raw guidance hashes against
hook-freed38c25f76:81/9/27 comparisons. Prior factor/calculation/RAM/TA verifiers
and the source-generation control pass all three. No performance claim.

## Historical LOG #174 scope stop and remaining goal

Do not start another predecessor trace, caller census, transform-site search,
camera guess or repeat capture automatically. The current bounded record
producer assignment is exhausted: coordinates are live-ins, not calculated in
this block. Further tracing requires a newly approved concrete target and stop
condition; this audit does not grant that authority to itself.

The remaining-goal audit still distinguishes implemented bounded scene/material
export and tested public-header adapter from real reconstruction. Current
remake_scene.cpp rejects unknown projection, transforms and normals; the public
adapter reports api-submitted-not-rendered-or-presented. The original M2 plan
requires moving native alignment plus camera/depth controls. Strict source
equality remains user-parked, not passed; this operand chain does not replace
whole-scene camera provenance or moving coverage. Real GPU/relighting/chaining
remain later unproven milestones, not successes inferred from this trace.

After checkpoint verification, audit current M2 requirements against actual
artifacts and return the exact unmet scope. If there is no remaining safe
implementation under current authority, request a new bounded route. Keep the
full goal incomplete; do not broaden tracing or shrink acceptance to the
already proven one-vertex path merely to close it.
