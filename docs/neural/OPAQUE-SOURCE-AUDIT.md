# FC-067 opaque-source coverage and bounded next witness

## Current disposition

Restored-source validation: four serial builds pass; three enabled selftests
pass284/284, SDK mock56/56 reports runtime/GPU/presentation false, inspector
suite65 and binary32 suite5 pass. Fresh hook-free `fc067-opaque-restored-j`
capture exits0/clean close, contains no temporary FC067 log, and matches27 unique
native planes and all three producer identities against the independent baseline.
No performance claim. This is accepted bounded evidence, not camera recovery.

The bounded candidate is UNCORRELATED to PVR, not impossible to recover. The
observed routine returns scalar arithmetic outputs; no opaque projection or
camera producer has been demonstrated. Temporary rec_x64 hooks were removed
after saving I's full patch outside Git. Production source now matches HEAD.
`opaque_routine_inspect.py` verifies the retained I path and rejects four offline
changes (edge, return, stop and read value). Four synthetic parser tests bring
the inspector suite to65 passing methods. These checks do not prove scalar
store bytes or whole-routine semantic classification.

Next close this evidence checkpoint and audit M2's remaining requirements
against existing artifacts. Do not resume arbitrary caller tracing, site census
or the parked replay residual. A further title-specific reconstruction expansion
needs an explicit bounded rationale and scope, not automatic continuation from
one uncorrelated scalar result. Main-scene camera and actual Remix GPU stay open.

## Working A executed observation

Read-result E: automation build/capture pass, three frames cleanly closed.
Six returned values match the pre-read source bytes exactly. The specific
consumer block8c048e2a contains20 operations, not truncated. SSA links later
reads8c048e4c/54/5c to stores8c048e50/58/60 at stack offsets4/8/12, preserving
z/y/x order rather than inventing an xyz projection. Source ends at the same
event537 overwrite. All27 unique native planes and producer stamps match the
independent no-reset baseline. Compiled dependencies alone do not prove stores;
the next F probe adds actual post-store addresses/bytes. F automation build
passes; exact temporary patch saved ignored as fc067-opaque-copy-f.patch.
F actual capture also exits0/clean close. Exactly three post-store records verify
PCs8c048e50/58/60 write source components2/1/0 to8c00f120/124/128, each actual
value equals committed RAM. All27 unique planes and producer identities match
the baseline. This is a new12-byte z/y/x copy, not a PVR packet or camera.
Next follow this copy generation's consumers under the same bounds, separately
from the original16-byte source lifetime; preserve the explicit component map.

Independent copy G: automation build and actual capture pass, three frames,
clean close. Copy generation8c00f120 has two observed pre-access reads, at
event3 PC8c048aa4 (address8c00f124, source y) and event5 PC8c048aaa
(address8c00f128, source x). No read of its z word is observed in this bounded
SHIL CPU trace. An overlapping write ends this generation at event536,
PC8c048e50/address8c00f120. No cap/expiry marker. This does not rule out
unobserved access mechanisms; it is not global no-consumer proof.
All27 unique native planes and producer identities match the baseline. The
full temporary G patch is saved ignored as fc067-copy-span-g.patch.

Next inspect the executed consumer at8c048aa4/8c048aaa and its bounded compiled
dependencies/returned values, not arbitrary new sites. Two x/y reads alone do
not identify rendering or camera semantics. If this terminates in CPU-only
arithmetic, document that candidate disposition rather than force a TA link.

Consumer H: build/capture exit0, three frames cleanly closed. Both returned
values match the copied y/x words exactly. The identified block8c048aa0 has12
compiled operations with no truncation. It reads another pair from r4+4/+8,
subtracts those from the copied pair, tests the first difference against zero,
and branches; the block contains no write or TA submission. This establishes
pair-difference arithmetic at the observed consumer, not a complete routine
classification or proof that later code cannot render. All27 unique image planes
and producer identities match the independent native baseline. Full temporary
H patch saved ignored as fc067-copy-consumer-h.patch.

The missing evidence is now the executed continuation/return of this specific
pair-difference routine. Any further probe must be bounded to that invocation
and terminate at return/event cap, not expand into a general guest decompiler.
Absent a causally observed rendering output, report this candidate uncorrelated;
do not infer camera from the transform's non-unit fourth component or from names.

Routine I: first build fails on missing sstream/locale includes; corrected I2
build passes. Actual capture exits0/clean close,27 unique planes and producer
identities remain exact. The one selected invocation executes blocks8c048aa0,
8c048ad2,8c048afe,8c048b1a and returns to the observed PR8c048e70 after four
blocks, below the16-block/0.1-second limits. Every recorded next PC agrees with
the following block and the final next agrees with saved return. All four
descriptors are present. Final r0 is2. The path computes differences, products,
a square root and two scalar stores at8c048b28/8c048b2c through r6; it does not
provide a captured projection or TA submission. The full temporary I patch is
saved ignored as fc067-routine-i.patch. End-of-block records plus their compiled
operations do not yet constitute independent post-store byte verification for
those two outputs; no such verification is claimed.

Disposition at this invocation boundary: still uncorrelated to PVR. This path
is scalar pair arithmetic, not a demonstrated opaque camera producer. Do not
follow arbitrary downstream callers indefinitely or promote CPU arithmetic to
camera recovery. Review the bounded evidence against the source-plan stopping
boundary before authorizing another lineage expansion. Whole M2 remains open.

Consumer D: automation build passes and actual `fc067-opaque-span-d` capture
exits0/clean close. A16-byte generation at8c00f180 has six observed pre-access
reads: events12/13/14 at PCs8c048df8/8c048dfe/8c048e08 and events23/25/27 at
8c048e4c/8c048e54/8c048e5c. It ends at an observed overlapping CPU store,
event537 PC8c048dce at8c00f18c. No time/event cap is reached. Exact source-store
verification still passes; all27 unique native planes and three producer stamps
match the no-reset baseline. This is CPU-access coverage only; no general DMA,
ABA or completed-read-result coverage is implied.

The first three reads are stack-relative offsets100/104/108. Their compiled
dependencies include x*x+y*y followed by sqrt and a z comparison; that is not
evidence of projection into a PVR packet. The later reads8c048e4c/54/5c are the
next concrete seam: record their actual returned values and immediate dependent
stores/operations before following another generation. Do not call this normal,
lighting, camera or opaque geometry merely from the arithmetic pattern.

Follow-up B/C: added pending-store age/backwards-clock/PC-range checks and
invalidation on an intervening transform. These are source guards, not an
exhaustive lifecycle/reset test. Added opt-in wrong-expected component0 bit;
only diagnostic expected data changes, never the actual transform or guest RAM.
Automation B build exits0. Guarded positive B and negative C each capture three
frames and close cleanly. Existing store verifier with explicit SITE8c048dcc
confirms identical input/matrix/output/cycle/address across runs, four exact
positive stores, and the intended wrong-expectation failure in negative C.
Passing B as both positive and negative correctly rejects. No invalidation
marker occurs in either accepted observation. All54 plane comparisons (27 per
run) and all six producer identities match the independent native baseline.
No TA/camera claim follows. Next trace consumers of this exact stored generation,
bounded by overwrite, time and event cap; do not repeat positive/negative runs.

Temporary environment-opt-in rec_x64 observer adapts the retained F mechanism
to site8c048dcc and stores8c048dce-dd4, first execution in38-40 emulated seconds.
Automation build `fc067-opaque-store-a-build.log` exits0. Actual capture
`fc067-opaque-store-a` exits0/clean close. Existing store inspector, with SITE
explicitly set to8c048dcc, verifies four ordered RAM stores at cycle7602209664.
All27 unique native image planes and all three producer identities match the
separate d2954272e no-reset baseline exactly. This is working diagnostic evidence,
not accepted opaque coverage: TA association and a fresh runtime wrong-output
control are still pending. No runtime negative is claimed from this positive run.

The source diagnostic remains dirty and must not ship as production camera code.
Before further lineage: add explicit pending-generation expiry/reset guards and
run the wrong-expected-word control, retaining actual source/output equivalence.
Then follow only this generation's consumers; stop on overwrite/absence/caps.
Initial saved-diff adaptation failed twice due stale hunk labels; a JS hunk
selection attempt also failed before modification. Normalizing full hunk headers
allowed apply_patch; only that successful patch changed source. No build failure.

Starting HEAD87492e1d3. Read-only review of retained source artifacts; no new
game capture, shader change, camera recovery or runtime integration in this slice.

## Actual combat coverage

The exact-SHA d2954272e native frame1782 packet contains3075 opaque draws,
25 punch-through draws and247 translucent source draws. Resolving indexed ranges
and excluding the explicit0xffffffff primitive-restart token yields:

| List | Unique referenced vertices | Referenced vertex range |
|---|---:|---|
|Opaque|14274|0-14767|
|Punch-through|100|15867-15966|
|Translucent|1099|14768-15866|

Opaque and punch-through ranges contain3032 and23 restart references respectively.
These are reference counts, not primitive/pixel coverage. The initial exploratory
count incorrectly included the restart token as a vertex; corrected by explicitly
excluding it and checking every remaining index against vertex count. No claim
uses the initial count. The proven fish vertex14770 occurs only in translucent
coverage. No opaque camera provenance follows from the two effect observations.
The packet explicitly retains unknown camera/world transform/Dreamcast normals.

## Existing executed candidate, not a new census

The final session of retained `fc067-ftrv-sites-working-d` contains exactly two
FC067_FTRV_SITE records, eight matrix-column records and83 compile-SHIL records.
Sites8c048dcc and8c048dde execute at cycle6000639232 with explicit uncorrelated
lineage. Their observed output includes a non-unit fourth component; that alone
does not identify camera, projection or physical depth.

The compiled block at8c048d60 links site8c048dcc's output f0v4.5 to four reverse
component stores: PCs8c048dce/dd0/dd2/dd4 write f3.5/f2.5/f1.5/f0.5 through
successively decremented r1 versions. Unlike a selected plausible matrix, this
is a concrete existing SSA store boundary. However the retained D run has no
post-store effective-address/bytes witness, no storage lifetime and no TA link.
Compilation records are not proof those particular stores executed in that run.

## Next bounded implementation

Adapt the retained F post-store observer (not its title-specific constants) for
one actual execution of8c048dcc in the combat producer interval. Observe all
four stores8c048dce-dd4, actual addresses and bytes; reject missing, repeated,
wrong-output or expired observations. Bound the observer to one generation and
the existing time/event caps. If the site does not execute in the selected
interval, record that absence and stop this candidate; do not expand the census.

Only after a verified store generation exists follow its actual consumers to
TA. Require same-context draw association before calling it opaque coverage;
another effect/CPU-only result is a valid rejection, not a camera success.
Retain a wrong-output negative and native-preservation comparison with matched
producer timing. No guessed projection, normal or world transform is permitted.

Failed read-only lookups for the nonexistent TRANSFORM-SPAN-AUDIT.md and a
PowerShell rg glob were corrected using existing files. A search for SAMPLE/
OUTPUT/STORE tags returned none because D uses SITE/MATRIX tags; actual tag
inventory above was then read. No live third-party configuration was touched.
